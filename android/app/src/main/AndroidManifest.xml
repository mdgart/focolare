<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Cook timers.

        POST_NOTIFICATIONS is the runtime prompt on Android 13+; without it
        nothing is shown at all.

        SCHEDULE_EXACT_ALARM is the one that matters for accuracy. Without it
        Doze batches alarms and a five-minute egg timer fires "some time after
        five minutes" — late, silently, with no error and no way for the cook to
        know until the food is ruined. The user can revoke it in Settings, which
        is why the app checks at runtime and says so rather than assuming.

        USE_EXACT_ALARM would grant the same thing with no prompt, but Play
        restricts it to apps whose core function is timing (alarm clocks,
        calendars). A recipe app with a timer is a plausible argument to make
        later; it is not one to make silently in a first submission.

        RECEIVE_BOOT_COMPLETED re-registers pending alarms after a restart —
        otherwise a phone that reboots mid-prove loses every timer.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
        Photographing a dish. READ_MEDIA_IMAGES is the Android 13+ replacement
        for READ_EXTERNAL_STORAGE, which is kept only for older devices.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
</manifest>
